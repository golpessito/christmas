<%= form_for(channel) do |f| %>
  <% if channel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(channel.errors.count, "error") %> prohibited this request from being saved:</h2>

      <ul>
      <% channel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row">
    <div class="col-md-1">
      <%= f.label :name %>
    </div>
    <div class="col-md-1">
      <%= f.text_field :name %>
    </div>
  </div><br>
  <div class="row">
    <div class="col-md-1">
      <%= f.label :description %>
    </div>
    <div class="col-md-1">
      <%= f.text_area :description %>
    </div>
  </div><br>
  <div class="row">
    <div class="col-md-1">
    </div>
    <div class="col-md-1">
      <%= f.submit %>
    </div>
  </div>
<% end %>
